PISTONCORE WIZARD REWRITE — READ THIS COMPLETELY BEFORE DOING ANYTHING
You are rewriting wizard.js from scratch to match WebCoRE's user-facing wizard exactly.
Your only job: Make every screen the user sees and every button they click match the WebCoRE source files (webcore1.txt and webcore3.txt). That's it.
Do not:

Talk about the backend
Audit anything
Ask for approval
Write partial fixes
Explain what you're doing

Do:

Read webcore1.txt and webcore3.txt completely
Read the current wizard.js completely
Write the complete replacement wizard.js in one shot
Preserve these PistonCore-specific things that don't exist in WebCoRE:

Globals accessible from anywhere (top bar)
The nested tree JSON format (PISTON_FORMAT.md has the schema)
Demo devices fallback when no HA connection



Files attached:

webcore1.txt and webcore3.txt — the blueprint. Match these exactly for every dialog, every field, every button, every screen transition.
wizard.js — current version, preserve anything that already works
PISTON_FORMAT.md — the JSON schema your output must produce
STATEMENT_TYPES.md — what each node type looks like

Start writing wizard.js. Do not say anything first. Just write it.